# 작업 이력 — 2026-09-18(금)

## 1. 키움 토큰 오류 → 키 교체 (C2-3)
- 증상: `토큰 발급 실패: 응답 없음`. 실제 원인은 모의투자 앱키 만료/무효(return_code 8001, HTTP 200으로 옴).
- `TokenManager.issue()` 검사 순서 변경: null 응답 → `return_code != 0`(return_msg·host 포함) → token 누락(키 목록 로그). 키 오류가 더 이상 "응답 없음"으로 가려지지 않음.
- `KiwoomProperties` compact 생성자에서 URL·키 공백/개행 제거.
- `.env` `KIWOOM_MOCK_G_*` 재발급값 반영, GitHub secrets 갱신 → CI 통과.

## 2. 모의투자 재개·정리
- 앱 RUNNING, WS 로그인 정상, 포지션 복원.
- `scripts/sql/20260918_cleanup_orders.sql`: UNKNOWN 주문(SELL-005) → REJECTED. UNKNOWN=0.
- 수동 매도 RC4003(호가단위) 거부 → `risk/KrxTickSize`(tickFor/isAligned/align, HALF_UP) 추가, `RiskGate`에서 OrderRequest 발행 직전 보정 + 테스트.
- 대시보드 수동주문 폼 기본값(가격=현재가, 수량 빈칸=자동 사이징 ~5,000만원) 때문에 005930 193주 의도치 않은 매수 → 초과 192주 보유 중. **9/21(월) 09:05 전 매도 필요(틱 500원)**. 게이트 ② 카운트 9/21 재시작.
- TR 실측: ka10001/ka10004 키 확정(`cur_prc, open_pric, high_pric, low_pric, stk_nm, sel_fpr_bid, buy_fpr_bid`), 폴백 키·1회성 키 로그 제거. ka10075 `oso` 배열(빈 응답 2회).
- 일일 리포트 9/18 누락: 앱이 12:34~16:36 꺼져 있었음(버그 아님).

## 3. 로그·콘솔
- 로그 경로 `D:\myApp\autoStock\app\logs\autostock.log` (`logback-spring.xml` 기본값 `logs/autostock.log`, `start_autostock.bat` LOG_FILE·mkdir).
- 한글 깨짐: `bootRun` jvmArgs `-Dfile.encoding/-Dstdout.encoding/-Dstderr.encoding=UTF-8`, bat `chcp 65001`.

## 4. 트랙 X — 횡단면 백테스트 (게이트 ① 전 계열 FAIL 대응)
- `docs/research/gate1_diagnosis_20260918.md`: 17계열 FAIL 원인 진단, 경로 A(횡단면) 채택, X1~X3 사전 선언.
- 엔진: `backtest/CrossSectionalStrategy`, `CrossSectionalStrategies`(momentum12_1, lowVolatility), `CrossSectionalBacktestRunner`(월말 형성·익일 시가 체결·동일가중·CostModel·상폐 청산·pendingExits·일수익률 40% 가드·시점별 시총 유니버스). 합성 테스트 6건 통과.
- 데이터: `fetch_universe_prices.py`(Yahoo 2,716/3,931), `fetch_krx_marketcap.py`(pykrx, KRX 로그인, 월말 141개 스냅샷), 스팩/리츠/펀드 167종목 제외.
- 결과: X1 −77.7%(거래대금 상위 유니버스=테마주 편향; Python 재현 일치) → X1b/X2b 시총 상위 200으로 교체해도 FAIL(+104%/MDD 58.5%, +170.7%/MDD 47.5%, SPA p=1.0 vs KODEX200 +451%/MDD 40.8%). 21계열 전부 FAIL.
- 구조적 결론: KODEX200 성과가 삼성전자·SK하이닉스 집중에 의한 것이라 동일가중 선별로 못 이김. 다음: X0 동일가중 진단 베이스라인, ADR-15(벤치마크), X3 F-Score×모멘텀, 이후 경로 C 결정.
- 주의: `.gitignore`의 `*.log` 때문에 `docs/measured/x_experiment_*.log`는 커밋 안 됨(.txt로 바꿔야 보존).

## 5. 외부 API 실측·활성화 (A2, 자택망)
- `scripts/probe_external_apis.ps1` → `docs/measured/ext_probe_20260918_*.json`.
- FRED: 포맷 확인(주석만). ECOS: item 코드 필수(731Y001/0000001 USD, 722Y001/0101000 기준금리), 14일 범위 조회, INFO-200=데이터 없음.
- 공휴일(data.go.kr): `%`가 cmd `set`에서 깨짐 → `load_env.ps1` `%`→`%%`; WebClient 재인코딩 → 수동 URI. 결과 "저장/갱신=6건, 실패=0개월"(추석 9/24~26 적재). 9/28 대체휴일 여부 KRX 확인 필요.
- DART `handshake_failure`: 서버가 TLS1.2 `TLS_DHE_RSA_WITH_AES_128_CBC_SHA256`만 협상, Netty 기본 cipher 목록에 DHE 없음 → `DartClient`/`MajorDisclosureDartClient`에 JDK 기본 cipher 목록 적용(`jdkCipherConnector`). IPO·블랙리스트(~40건) 동기화 정상.
- `application.yml`: holiday-api·macrointel·blacklist·dart enabled=true (telegram false — A3 봇 미발급).

## 6. 따라잡기(catch-up) 스케줄
- Holiday/Macro/Ipo/DisclosureBlacklist 4개 스케줄러: `ApplicationReadyEvent` 기동 시 + 매시 05분(`0 5 * * * *`) 확인, 당일 성공 시 스킵, enabled=false·키 공백 시 스킵.

## 7. Swagger
- `springdoc-openapi-starter-webmvc-ui:2.8.9` 추가, `springdoc:` 설정(`/api/**`). http://localhost:8080/swagger-ui.html
- 공모주 반수동 호출: `GET /api/ipo?status=`, `POST /api/ipo/{id}/metrics`(기관경쟁률·확약비율), `POST /api/ipo/{id}/record`(청약/배정/매도 부분 갱신). 상장일 수동 입력 API는 없음(후속 검토).

## 8. 운영 교훈
- 에디터 탭이 열린 파일은 내 쓰기가 덮어써짐(PROGRESS.md ×3, DartClient ×1) → 쓰기 전 탭 닫기, 재스테이징으로 검증.
- 채팅에 KRX 비밀번호·data.go.kr 키 노출 → 변경/재발급 필요.
- 변경은 한 번에 하나씩 확인.

## 남은 일
- [ ] `.\gradlew :app:test` 후 오늘 변경 전부 커밋/푸시
- [ ] 주말 전 `stop_autostock.bat`
- [ ] KRX 비밀번호 변경, data.go.kr 서비스키 재발급 → `.env`
- [ ] 9/28 대체휴일 KRX 확인(맞으면 market_holidays 추가)
- [ ] 9/21(월) 09:05 전 005930 초과 192주 매도
- [ ] 텔레그램 봇 발급(A3) → `TELEGRAM_BOT_TOKEN`, telegram enabled
- [ ] PROGRESS.md에 DART TLS 원인/조치·catch-up·Swagger 항목 추가(탭 닫힌 뒤)
- [ ] 트랙 X: X0 → ADR-15 → X3
