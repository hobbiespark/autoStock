# 작업 이력 — 2026-09-23(수)

## 로그 점검 결과(9/19·9/22 압축분, 9/23 현재분)
- 9/19 12:00 이후 로그 공백 → 9/22 17:26 복귀(PC 절전, 앱은 주말 내내 실행 상태). 복귀 직후 `StaleOrderCanceller`(1분 fixedRate) 밀린 회차 215건이 동시 실행되어 Hikari 풀 고갈(active=10, waiting=210). 코드 누수 아님 — 절전 + 스케줄 폭주.
- 9/23 09:05 C3: 005930·035420 판단 스킵 — ka10081 `[1700] 유량=1` 초과(HTTP 200 + return_code 5라 429 재시도 미작동). 15:45 분봉 적재(ka10080) 3종목 동일 실패.
- 9/23 07:53 대시보드 500 × 30건 — ka10001 `[1631]/[1632]` 서버측 오류(장전). 미조치.
- 9/23 C3 매수: 000660 9주 @1,889,000 / 069500 262주 @114,000. 005930 초과 192주 미정리.
- 정상: FRED/ECOS, 공시 블랙리스트 30건, IPO 따라잡기, WS 재접속, 15:50 리포트.

## 적용(테스트 통과: `:app:test --rerun` BUILD SUCCESSFUL)
1. `kiwoom/TrRateLimiter`: TR당 버킷 2건/초 → 1건/초(서버 유량 실측).
2. `kiwoom/KiwoomRestClient`: `[1700` 유량 초과 논리 오류도 재시도(간격 1.1초), 1회 호출 `REQUEST_TIMEOUT` 15초(타임아웃은 재시도 안 함 — 주문 중복 방지).
3. `monitor/DailyReportScheduler`: `BrokerPort.balance()`(kt00018) 연결 — `계좌 평가액(추정예탁자산) / 보유 총평가 / 총평가손익`. 실패 시 사유만 표기. 테스트 2건 추가.
4. `trading/StaleOrderCanceller`·`ReconciliationService`: `fixedRate` → `fixedDelay`(절전 후 밀린 회차 1회로 합침).
5. `application.yml`: JDBC `?socketTimeout=30`, `hikari.connection-timeout 10s / validation-timeout 3s / leak-detection-threshold 60s`.
6. Swagger(springdoc 2.8.9) — http://localhost:8080/swagger-ui.html, `/api/**`.

## 남은 일
- [ ] 커밋/푸시 → 재기동 → 기동 로그 `HikariPool-1 - Start completed` 확인
- [ ] 내일 09:05 로그에 `판단 중 오류` 없는지, 15:50 리포트에 계좌 평가액 표시되는지
- [ ] 대시보드 ka10001 1631/1632 → 500 대신 이전값/빈값 반환(낮음)
- [ ] 005930 초과 192주 정리(틱 500원)
- [ ] 9/28 대체휴일 KRX 확인 → market_holidays
- [ ] 절전 정책: 장중 운영 PC 절전 해제 또는 퇴근 시 `stop_autostock.bat`
- [ ] KRX 비밀번호 변경, data.go.kr 키 재발급, 텔레그램 봇(A3), PROGRESS.md 반영
